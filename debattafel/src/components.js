import {categories} from './services/news-service.js';import {isFavorite,toggleFavorite} from './services/favorites.js';
export const escapeHtml=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
export function empty(title,text,action=''){return `<section class="empty"><span class="badge">Redactie-status</span><h3>${escapeHtml(title)}</h3><p>${escapeHtml(text)}</p>${action}</section>`}
export function toolbar(){return `<div class="toolbar"><label><span class="skip">Zoeken</span><input id="search" type="search" placeholder="Zoek op onderwerp, bron of tag"></label><select id="category"><option value="">Alle categorieën</option>${categories.map(c=>`<option>${c}</option>`).join('')}</select><select id="sort"><option value="newest">Nieuwste</option><option value="importance">Belangrijkheid</option><option value="debate">Debatwaarde</option></select></div>`}
export function card(a){return `<article class="card"><div class="card-image" role="img" aria-label="${escapeHtml(a.imageAlt||'Artikelafbeelding')}"></div><div class="card-body"><span class="badge">${escapeHtml(a.category)}</span><h3>${escapeHtml(a.title)}</h3><p>${escapeHtml(a.summary)}</p><p class="meta">${escapeHtml(a.source)} · ${new Intl.DateTimeFormat('nl-NL',{dateStyle:'medium'}).format(new Date(a.publishedAt))}</p><div class="chips">${(a.tags||[]).map(t=>`<span class="chip">${escapeHtml(t)}</span>`).join('')}</div><p><a class="button" href="/debat/${encodeURIComponent(a.slug)}" data-link>Debatmodus →</a> <button class="button secondary favorite" data-id="${escapeHtml(a.id)}">${isFavorite(a.id)?'Opgeslagen':'Bewaar'}</button></p></div></article>`}
export function bindFavorites(items){document.querySelectorAll('.favorite').forEach(b=>b.onclick=()=>{const a=items.find(x=>x.id===b.dataset.id);if(a){b.textContent=toggleFavorite(a)?'Opgeslagen':'Bewaar';document.querySelector('#favorite-count').textContent=`(${JSON.parse(localStorage.getItem('debattafel:favorites')||'[]').length})`}})}
