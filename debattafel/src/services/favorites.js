const KEY='debattafel:favorites';const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return []}};export const favorites=()=>read();export const isFavorite=id=>read().some(x=>x.id===id);export function toggleFavorite(item){const all=read(),exists=all.some(x=>x.id===item.id);localStorage.setItem(KEY,JSON.stringify(exists?all.filter(x=>x.id!==item.id):[...all,{...item,savedAt:new Date().toISOString()}]));return !exists}
